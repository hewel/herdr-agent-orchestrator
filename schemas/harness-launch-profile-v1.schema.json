{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://herdr.dev/schemas/harness-coordinator/harness-launch-profile-v1.schema.json",
  "title": "HarnessLaunchProfileV1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "id", "kind", "executable", "provider_profile", "inherit_env", "config_overlays"],
  "properties": {
    "schema_version": { "const": 1 },
    "id": { "$ref": "common-v1.schema.json#/$defs/harnessId" },
    "kind": { "enum": ["omp", "codex"] },
    "executable": { "$ref": "common-v1.schema.json#/$defs/absolutePath" },
    "provider_profile": { "type": "string", "minLength": 1, "maxLength": 128 },
    "model": { "type": "string", "minLength": 1, "maxLength": 256 },
    "inherit_env": {
      "type": "array",
      "maxItems": 128,
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[A-Z_][A-Z0-9_]{0,127}$" }
    },
    "config_overlays": {
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "common-v1.schema.json#/$defs/absolutePath" }
    }
  },
  "allOf": [
    {
      "if": { "properties": { "kind": { "const": "codex" } } },
      "then": { "properties": { "config_overlays": { "maxItems": 0 } } }
    }
  ]
}
